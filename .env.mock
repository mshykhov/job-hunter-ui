# Local UI preview with mocked API (no backend, no Auth0).
# Run: npm run dev:mock
VITE_ENABLE_MOCKS=true
AUTH0_ENABLED=false
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_AUDIENCE=
API_URL=http://localhost:8095
